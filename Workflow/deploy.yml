name: 'Deploy to AWS CloudFormation'

on: 
  push:
    branches: [ master ]
  # pull_request:
  #   branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  # This workflow contains a single job called "deploy"
  deploy:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout code from master branch
        uses: actions/checkout@v2

      # Configure AWS Credential
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.ACCESS_KEY_SECRET }}
          aws-region: us-east-1

      # # Deploy to AWS CloudFormation test-stack
      # - name: Deploy test-stack to AWS CloudFormation
      #   uses: aws-actions/aws-cloudformation-github-deploy@v1
      #   with:
      #     name: cicd-test-stack
      #     template: Webserver-on-EC2-cloud-formation-template.yaml
      #     no-fail-on-empty-changeset: "1"
      #     parameter-overrides: >-
            AccessToken=${{ secrets.ACCESS_TOKEN }},
            Repository=https://github.com/Ula2022/wildrydes-site



   # Push changes to repository
      - name: Push changes to repository
        shell: bash
        run: |
          git config --global user.email "oogunlesi2002@yahoo.co.uk"
          git config --global user.name "Olakunle Ogunlesi"

          echo "machine github.com login Ula2022 password ${{ secrets.ACCESS_TOKEN }}" > ~/.netrc

          git clone https://github.com/Ula2022/Webserver-on-EC2-cloud-form.git
          cd Webserver-on-EC2-cloud-form


          
          git add -A
          current_date_time=$(date)
          git diff --quiet HEAD || git commit -m "config updated: $current_date_time"
          git push      